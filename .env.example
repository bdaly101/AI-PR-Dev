# GitHub App Configuration
# Required: Your GitHub App ID (found in GitHub App settings)
GITHUB_APP_ID=123456

# Required: GitHub App private key (PEM format, with \n for newlines)
# Example: GITHUB_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\nMIIEpAIBAAKCAQEA...\n-----END RSA PRIVATE KEY-----\n"
GITHUB_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\n...\n-----END RSA PRIVATE KEY-----\n"

# Required: Webhook secret for validating GitHub webhook signatures
# Generate a secure random string (e.g., using openssl rand -hex 32)
GITHUB_WEBHOOK_SECRET=your_webhook_secret_here

# AI Provider Configuration
# Required: OpenAI API key for GPT-4
OPENAI_API_KEY=sk-proj-...

# Optional: Anthropic API key for Claude fallback
# If not provided, only OpenAI will be used
ANTHROPIC_API_KEY=sk-ant-...

# Server Configuration
# Optional: Server port (default: 3000)
PORT=3000

# Optional: Node environment (default: development)
# Options: development, staging, production
NODE_ENV=development

# Database Configuration
# Optional: SQLite database path (default: ./data/app.db)
# For production, use an absolute path like /app/data/app.db
DATABASE_PATH=./data/app.db
